import React, { Suspense } from 'react'
import { Navbar, SliderContainer, ToggleButton } from "@/components";
import EffectRenderer from "@/components/EffectRenderer";

const Dashboard = () => {
  return (
    <main className='dashboard-page w-full h-auto bg-[#131517]'>
    </main>
  )
}

export default Dashboard
